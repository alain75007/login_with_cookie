<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.questioncode.myschool.LoginActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/act_login_title"
        android:text="@string/act_login_title"
        style="@style/h1"/>

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textEmailAddress"
        android:ems="15"
        android:id="@+id/act_login_email"
        android:layout_below="@+id/act_login_title"
        android:layout_marginTop="20dp"
        android:hint="@string/act_login_email_hint" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:ems="15"
        android:id="@+id/act_login_password"
        android:layout_below="@+id/act_login_email"
        android:layout_marginTop="20dp"
        android:hint="@string/act_login_password_hint" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/act_login_submit_btn"
        android:id="@+id/act_login_submit_btn"
        android:layout_below="@+id/act_login_password"
        android:layout_marginTop="20dp" />

</RelativeLayout>
